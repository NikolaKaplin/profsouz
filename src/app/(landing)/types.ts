export type ProfsouzHistory = {
  name: string;
  content: string | [];
};

export type DocumentsInfo = {
  name: string;
  content: string[];
  link: string[];
};

export type BenefitItem = {
  id: string;
  number: string;
  title: string;
  description: string;
};
